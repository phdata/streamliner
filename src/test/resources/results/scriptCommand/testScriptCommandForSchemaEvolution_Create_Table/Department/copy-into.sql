USE DATABASE SANDBOX_POC1;

USE SCHEMA EMPLOYEES;

COPY INTO Department (DEPT_ID)
FROM ( SELECT

	$1:DEPT_ID::NUMBER(38, 0)
FROM @STREAMLINER_QUICKSTART_1_stage/Department)

	FILE_FORMAT = ( TYPE = PARQUET);